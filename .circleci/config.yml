version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
     
    steps:
      - checkout
      - run: echo 'Hello World!'
          
workflows:
  version: 2
  build_1:
    jobs:
      - build