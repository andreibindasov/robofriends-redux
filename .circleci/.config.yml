version: 2.1
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
      # - image: alpine:3.7
    steps:
      - checkout
      - run: echo 'Hello World!'
          # name: The First Step
          # command: |
          #   echo 'Hello World!'
          #   echo 'This is the delivery pipeline'